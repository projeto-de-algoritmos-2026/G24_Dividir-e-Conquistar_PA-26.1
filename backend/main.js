const userRank = [];

// carrega as musicas
async function init() {
    const data = await loadAlbum;
    renderAvaliable(data.tracks);
}

// depois faço
function renderAvaliable(tracks) {
    return
}